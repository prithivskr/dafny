# Dafny

Submission for CS 474 spring 2026 project. Build with `make exe` and run `./Binaries/Dafny` with `--qf-heap` and `--p3` flags.

This modified version of Dafny produces no quantification over the heap.